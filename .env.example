# Copy this file to .env for your local lab, or pass it explicitly with --env-file.
# Do not commit real .env files.

# VMware vmrun.exe path. Keep this only if your VMware install path is non-standard.
VMRUN_PATH="C:\Program Files (x86)\VMware\VMware Workstation\vmrun.exe"

# Default guest credentials used by VMware guest operations.
# Override per-VM via VMWARE_CREDENTIALS_FILE (see below).
VMWARE_GUEST_USER=Administrator
VMWARE_GUEST_PASSWORD=change_me

# Optional: per-VM credentials file (JSON).
# Key = VM filename stem (without path and .vmx), or full VM path.
# Example credentials.json:
# {
#   "Windows 11 x64": {"user": "19657", "password": "admin123"},
#   "Win10": {"user": "admin", "password": "pass456"}
# }
VMWARE_CREDENTIALS_FILE=credentials.json

# Optional vmrest/MCP credentials if you also use the VMware MCP server.
VMWARE_HOST=localhost
VMWARE_PORT=8697
VMWARE_USERNAME=
VMWARE_PASSWORD=
